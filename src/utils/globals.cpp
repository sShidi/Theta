#include "../../include/utils/globals.hpp"

/**
 * Global symbols for processor return values
 *
 * Definition of global variables declared in globals.hpp
 * These are initialized in polyDBM_wrapper::Init()
 */

// Definition (actual storage allocation)
Napi::String noopSym;
Napi::String removeSym;